{
  "name": "SystemAdmin-tool",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd Linux-tools && npm run dev",
    "build": "cd Linux-tools && npm run build",
    "lint": "cd Linux-tools && npm run lint",
    "preview": "cd Linux-tools && npm run preview",
    "install-deps": "cd Linux-tools && npm install"
  }
}
